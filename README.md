# あいサポ Ver.2.2

Ver.2.1.5 を基準に、活動エリア権限・地図中心UI・党員/サポーター名簿取り込みを追加した版です。

## Ver.2.2 の主な変更

### 1. 活動エリアのアクセス制御
- 一般利用者（member）は `Users.areaId` に固定活動エリアを持ちます。
- 一般利用者には、自分の固定エリア以外の `Records` / `Contacts` をApps Script側から返しません。
- 支部管理者（leader）は自支部内の活動エリアを切り替えられます。
- 県連管理者・システム管理者は利用可能な全エリアを切り替えられます。
- 管理画面から一般利用者の固定エリアを変更できます。

### 2. 地図中心UI
- トップの登録数・訪問済・再訪のカードを削除し、「分析」へ移動しました。
- 地図の高さを拡大しました。
- 「党員・サポーターを優先表示」フィルタを追加しました。
- 訪問ピンに、名簿と紐づいている場合 `⭐党員` / `🟠サポーター` の印を表示します。

### 3. 名簿拡張 / Excel・CSV取り込み
Contactsに以下を追加しました。
- email
- memberType (`party_member` / `supporter` / `general` / `unknown`)
- lat / lng

管理権限のあるユーザーは名簿画面から `.xlsx` / `.xls` / `.csv` を取り込めます。
代表的な日本語列名（氏名、住所、電話番号、メールアドレス、区分など）を自動判定します。
ファイル全体を「党員」「サポーター」と指定して取り込むこともできます。

同一エリア内で `氏名 + 住所 + 電話 + メール` が同じものは重複登録をスキップします。

## 地図上の名簿表示について
Excelに住所だけ入っている場合、名簿一覧にはすぐ反映されますが、地図にピンを置くには緯度・経度が必要です。
名簿編集画面の「住所から地図位置を取得」で1件ずつ位置を保存できます。
無料のOpenStreetMap/Nominatimへ大量の住所を一括送信しないよう、Ver.2.2では大量一括ジオコーディングは実装していません。

## 更新手順
1. `gas/Code.gs` をApps Scriptへ上書きして保存。
2. Apps Script上で `upgradeV22` を **1回だけ** 実行。
3. Webアプリの既存デプロイを「新バージョン」で更新。
4. GitHubの `v2-dev` に `index.html` / `assets/` / `gas/Code.gs` / `README.md` を上書き。
5. ブラウザを再読み込み。

## 注意
- `Users` / `Contacts` / `Records` が入ったスプレッドシート自体を一般利用者へ共有しないでください。
- 画面上で隠すだけでなく、Apps Script側でもエリアアクセスを検証します。
- `passwordHash` と `salt` は削除しないでください。スプレッドシート上では列を非表示にして構いません。


## Ver.2.3
- 現行の党員名簿列（参政党ID、姓・名・カナ、電話、郵便番号、住所、党員種別、生年月日、性別、職業、承認日、支部参加、入党理由、支部）をExcel/CSVから取り込み可能。
- 名簿一覧・訪問先詳細に党員／サポーター区分を表示。
- 通常の訪問先にも電話・メールを保存可能。
- 一般利用者は固定活動エリア、支部管理者は支部内、県連／システム管理者は許可範囲のデータのみ取得する既存権限制御を継続。
- 既存環境では Apps Script の `upgradeV23()` を1回実行してください。


## Ver.2.4.2
- 地図の住所検索・検索・現在地・更新を1行に集約
- 地図凡例を党員/サポーターと訪問状態で同じ場所に統合
- 一覧フィルターを複数選択式へ変更
- 訪問注意（理由・メモ）を追加
- 訪問注意を地図ピン、一覧、フィルターで表示


## Ver.2.4.2
- 訪問注意の保存不具合を修正。Records列が旧構成でも自動整列します。
- 「断られた」を × 付きピンで表示し、地図・一覧フィルターに追加。
- 訪問詳細に「紹介者」を復活。紹介者も一覧検索対象です。
- 地図フィルターを全訪問状況に拡張。
- フィルターは同グループ内OR、グループ間ANDです。
- Apps Script更新後に `upgradeV232()` を1回実行してください。


## Ver.2.4.2
- Records保存を列番号依存から「ヘッダー名で保存」に変更。
- 既存の列ズレ行を修復する `repairRecordsV233()` を追加。
- 修復前に `Records_backup_before_v233` を自動作成。
- `repairRecordsV233()` は phone列に訪問状態、email列に対象種別が入った行だけを修復。
- 保存後に status / type / warning / referrer を再読込して検証。
- 一覧でTRUE/FALSEをそのまま表示しないよう整理。


## Ver.2.4.2
- Records修復を一律列移動から、実データの壊れ方ごとの判定方式に変更。
- `phone=訪問状態 / email=対象種別` の行だけ status/type を復元。
- `referrer=TRUE/FALSE / supporter=自由記述` の行だけ warning/warningMemo を復元。
- 既存の正常行は触りません。
- 修復前に `Records_backup_before_v234` を自動作成。
- Apps Script更新後に `repairRecordsV234()` を1回だけ実行してください。


## Ver.2.4.2 clean rebuild
- GASは正常だったVer.2.3を土台に再構築。
- saveRecord_をヘッダー名保存方式へ変更。
- 保存後に phone/email/status/type/referrer/warning/warningReason/warningMemo を再検証。
- repairRecordsV235() は既知の壊れたパターンだけ修復。
- 修復前にRecords_backup_before_v235を自動作成。


## Ver.2.4.2
- 訪問先の名簿紐づけをチェックボックス式に変更。
- 逆ジオコーディング住所を日本向けに整形し、Nominatimのdisplay_nameをそのまま保存しない。
- 「名前・呼び名」を「訪問先名」に変更。
- 訪問登録画面に「現在地から住所取得」を追加。
- 訪問登録画面に「Googleマップで確認」を追加。緯度経度があれば座標、なければ住所で開く。


## Ver.2.4.2 保存・読込の全項目整合
- Recordsをヘッダー名基準で正規化する upgradeV237() を追加。実行前にバックアップを自動作成。
- 保存後検証を、訪問画面で入力する全主要項目へ拡張。
- 名簿紐づけ時、保存・詳細画面に「⭐党員 / 🟠サポーター」、氏名、参政党ID、所属を明示。
- 一覧にも名簿区分を表示（既存の表示を維持）。


## Ver.2.4.2
- 地図未初期化時の `invalidateSize()` エラーを防止。
- 名簿紐づけは党員・サポーターのみを候補表示し、名前・住所検索を追加。
- 候補ゼロ時は案内文を表示。「名簿と紐づけない」の選択肢は廃止。
- 紐づけチェックONなのに名簿未選択の場合は保存前に案内。
- 保存中表示とエラーのコンソール出力を追加。
- 訪問先の削除ボタンは既存データ編集時だけ表示。


## Ver.2.4.2
- 保存後検証の date 比較を日付正規化方式に変更。
- `2026-08-13` / `2026/08/13` / Google SheetsのDate型を同じ日付として扱います。


## Ver.2.4.2
- 保存後の項目別文字列比較を廃止し、保存行とIDの存在確認に変更。
- phone/email/ID等はGoogle Sheets側でプレーンテキストとして保存。
- Recordsは引き続き列番号ではなくヘッダー名で保存するため、画面項目と列位置のずれを防止。


## Ver.2.4.2
- 新規訪問先の訪問日は今日の日付を自動設定。
- 既存訪問日は Date型・`yyyy/mm/dd`・`yyyy-mm-dd` をHTML日付入力用に正規化。
- 訪問先の名簿紐づけ候補判定を改善（党員・サポーター）。
- 保存の横にキャンセルを追加。
- 削除は既存訪問先だけ、保存・キャンセルとは別段に表示。


## Ver.2.4.2
- 名簿取り込み時に住所から登録済み活動エリアを自動判定。
- Apps ScriptのMaps Geocoderで住所から緯度・経度を取得し、党員/サポーターを地図へ表示可能に。
- エリアが一意に判定できない住所は誤配属せず、未取込として件数表示。
- インポート結果に位置取得成功/失敗・エリア判定不可・重複件数を表示。

## Ver.2.4.9
- 名簿取り込みで地図位置（緯度・経度）の取得に失敗しても、活動エリアを判定できる行はContactsへ登録します。地図ピンだけ表示されません。
- 名簿タブの一覧は初期状態で閉じ、必要なときだけ「名簿一覧を表示」で開く方式に変更しました。検索時は自動表示します。
- 一般利用者には、党員・サポーター名簿の正確な住所・電話・メール・緯度経度などをApps Script側から返さないようにしました。住所は丁目程度までの簡略表示です。
- 一般利用者は党員・サポーター名簿そのものを編集できません。通常の戸別訪問Recordは従来どおり詳細住所を表示・編集できます。
- 名簿編集の「表示名・呼び名」を「氏名・名称」に変更しました。

### 2.4.3 更新手順
1. `gas/Code.gs` をApps Scriptへ上書きして保存。
2. 今回はシート列追加がないため、アップグレード関数の実行は不要です。
3. Webアプリを新バージョンとしてデプロイ。
4. GitHub側の `index.html` / `assets` / `gas/Code.gs` / `README.md` を上書き。
5. ブラウザを再読み込み。


## 2.4.3 一覧検索修正
- Excel/CSVから取り込んだ党員・サポーターで、まだ訪問記録がない名簿も「一覧」に未訪問として表示・検索できるように修正。
- 地図位置を取得できていない名簿も一覧検索の対象。
- 訪問記録と紐づいた名簿は重複表示しない。


## Ver.2.4.9
- Contacts と Records の二重管理を整理し、新規名簿取込は Records に直接登録
- 旧 Contacts のデータは `upgradeV244()` で Records に一度だけ移行
- 登録方法を `名簿取込 / 手入力 / 地図登録` で保持
- 区分を `党員 / サポーター / 一般 / 未設定` で保持
- 一覧に「登録方法」「区分」「位置取得状態」の絞り込みを追加
- 位置取得に失敗しても Records に登録し、一覧から確認可能
- 名簿タブの検索・名簿一覧・ファイル全体区分を廃止
- 「＋名簿」を「＋新規登録」に変更


## Ver.2.4.9
### Records一本化の修正
- Records に `source`（登録方法）と `memberType`（区分）を正式列として追加
- `source`: `import` / `manual` / `map`
- `memberType`: `party_member` / `supporter` / `general` / `unknown`
- 名簿取込・手入力・地図登録のすべてを Records に保存
- 名簿再取込時、同一人物はスキップではなく既存 Records を更新
- 2.4.4までに区分が落ちたデータは旧 Contacts と照合して復元
- 住所の位置取得に失敗しても Records には残す

### 一覧
- 登録方法、区分、位置取得状態で検索
- 名簿取込 / 手入力 / 地図登録を区別

### 実戦向け分析
- 今日の優先対応：要再訪 / 未訪問 / 位置未取得 / 訪問注意
- 優先対応カードを押すと一覧を自動絞り込み
- 活動進捗率
- 党員 / サポーター / 一般
- 登録方法別件数
- 町丁目別の未訪問・再訪・位置未取得・進捗率

### 更新手順
1. `gas/Code.gs` を Apps Script に上書き
2. Apps Script で `upgradeV245()` を一度だけ実行
3. Apps Script を新バージョンでデプロイ
4. GitHub 側を ZIP 内のファイルで上書き


## Ver.2.4.9
- 選択中の活動エリアだけを一覧・地図・分析対象にする安全チェックを強化
- 名簿取り込み時、住所が登録済み活動エリアに判定できないデータを現在選択中エリアへ誤登録しない
- 住所なしの名簿だけは現在選択中エリアに登録可能
- 地図フィルターの右側に「該当 ○件」を表示し、絞り込みのたびにリアルタイム更新
- `setupFukuokaTestDataV246()` を追加
  - 福岡第1支部：東区・博多区
  - 福岡第2支部：中央区・南区・城南区
  - 福岡第3支部：早良区・西区・糸島市
  - 既存の東区や第1支部は重複させず更新
- 城南区西部の第3支部境界は町丁目単位のため、今回のテストでは城南区を第2支部扱い

### 更新手順
1. `gas/Code.gs` を上書き
2. 2.4.5の移行をまだしていない場合は `upgradeV245()` を一度実行
3. `setupFukuokaTestDataV246()` を一度実行
4. Apps Scriptを新バージョンでデプロイ
5. GitHub側をZIP内のファイルで上書き
6. 同梱の `aisapo-v2.4.6-testdata.xlsx` でエリア振り分けを確認


## Ver.2.4.9
- 支部IDを `fukuoka_1 / fukuoka_2 / fukuoka_3` に統一
- Areas / Users / Records の branchId を連動更新
- 既存Recordsを住所から再判定して正しい活動エリアへ再配属
- 東区選択時に、以前誤って東区へ保存された博多区・中央区等が混ざる問題を修正

### 必須
Apps Script の `Code.gs` を上書き後、`upgradeV247()` を一度だけ実行してください。
その後、新バージョンでデプロイし、GitHub側を更新してください。


## Ver.2.4.9
- 一般ユーザー向けに位置情報を伏せた党員・サポーターの `lat/lng=''` を地図が `0,0` と解釈する不具合を修正
- 緯度・経度が空欄・0・不正値のデータは地図ピンを表示しない
- これにより博多区などで地図が海上（0,0）へ移動する問題を解消


## Ver.2.4.9
- 地図の件数を「条件に該当したRecords件数」と「実際に地図表示できたピン件数」に分離
- 例：一般ユーザーで党員位置を非表示にしている場合は「該当 1件（地図表示 0件）」
- 一般ユーザーで党員・サポーターの位置を伏せている場合、一覧の表示を「位置未取得」ではなく「🔒位置非表示」に変更
- 地図の該当件数バッジを一回り小さく調整

## Ver.2.5.0
- 訪問先画面の「党員・サポーター名簿と紐づける」を削除
- 「名簿」タブは管理者権限のみ表示。一般利用者にはタブ自体を非表示
- 名簿取り込みAPIにも管理者権限チェックを追加
- 一般利用者にも党員・サポーターの住所・電話番号を表示
- 電話番号は一般利用者では閲覧専用
- 位置確認済みの党員・サポーター住所は一般利用者からの変更をGAS側で拒否
- 位置未確認の党員・サポーター住所は一般利用者でも修正可能
- 緯度経度・メール等は一般利用者には返さない

## Ver.2.5.1
- 一般ユーザー向けに `locationConfirmed` を返し、位置確認済み／未確認を正しく区別
- 位置未確認の党員・サポーターは住所編集可能
- 「住所から位置取得」ボタンを追加し、住所修正後に位置を再取得して保存可能
- 位置確認済みの党員・サポーターは住所欄を読み取り専用
- 一覧の表示を「位置確認済／位置未確認」に修正

## Ver.2.5.2
- 一般ユーザーで「位置確認済みなのに地図ピンが出ない」矛盾を修正
- 党員・サポーターも位置確認済みなら地図ピンを表示
- 位置確認済み：住所は閲覧のみ、地図ピン表示
- 位置未確認：住所を修正可能 → 住所から位置取得 → 保存後は位置確認済み
- 電話番号は一般ユーザーでは閲覧のみ
- メール等の不要な個人情報は引き続き一般ユーザーには非表示

## Ver.2.5.3
- 名簿取込データは一般ユーザーから削除不可（削除ボタン非表示）
- 管理者は名簿取込データを物理削除せず「無効化」
- 無効化は active=false として履歴を保持し、一覧・地図・分析から除外
- 手入力・地図登録データは従来どおり削除可能
- `upgradeV253()` を一度実行して既存Recordsの active を初期化

## Ver.2.5.4
- 名簿取込（source=import）の党員・サポーターは、一般ユーザーでは氏名も閲覧専用
- GAS側でも一般ユーザーによる氏名変更を拒否し、画面操作以外からの変更も防止
- 電話番号は従来どおり閲覧専用
- 住所は位置未確認のときだけ一般ユーザーでも修正可能
- 位置確認済み住所は閲覧専用

## Ver.2.5.5
### 訪問先の位置操作
- 地図から登録し、位置情報が確定している訪問先は「Googleマップで確認」だけ表示
- 名簿取込で位置未確認の場合は「住所から位置取得」を表示
- 手入力で位置未確認の場合は「住所から位置取得」「現在地から住所取得」を表示
- 位置が確定しているデータを住所検索で誤って上書きしにくいUIに整理

### ポスター管理
- 訪問先Recordsとは別に `Posters` シートで管理
- 「ポスター」タブを追加
- 状態：依頼中 / 掲示済 / 貼替え必要 / 撤去 / 断られた
- 掲示先・住所・協力者・電話・掲示日・確認日・貼替え必要・メモを登録
- 住所から位置取得 / 現在地から取得 / Googleマップ確認に対応
- 地図フィルターに「🪧ポスター」を追加
- ポスター表示時は地図の該当件数にもポスター件数を表示
- 一般ユーザーも固定活動エリア内のポスター情報を登録・編集可能
- `upgradeV255()` を一度実行して Posters シートを作成

## Ver.2.5.6
- ポスターの「貼替え必要」チェックボックスを削除
- 「貼替え必要」はポスター状態の1つとして一本化
- 一覧の🔄表示、地図アイコン、保存データは status='replace' を基準に判定
- 既存の replaceNeeded 列は互換性のため残すが、状態から自動設定

## Ver.2.5.7
- ポスター種別に「自党ポスター / 他党ポスター」を追加
- 他党ポスターでは「党名・政治団体名」を記録可能
- 他党ポスターは状態管理を簡略化し「掲示確認」として記録
- 地図フィルターを「🪧自党ポスター」「🏳️他党ポスター」に分離
- 地図アイコンも自党🪧 / 他党🏳️で区別
- 既存ポスターは `upgradeV257()` で自党ポスターとして初期化

## Ver.2.5.8
- 地図の自党ポスターアイコンを 🍊 に変更
- 他党ポスターは 🏳️ のまま
- 自党ポスターの「貼替え必要」は 🔄🍊 で表示
- 地図フィルター・凡例も 🍊自党 / 🏳️他党 に統一

## Ver.2.5.9
- ポスタータブを開くたびにサーバーから登録済みポスターを再読み込み
- ポスタータブに登録件数を表示
- 各ポスターカードに「編集」ボタンを追加
- Postersの保存を列番号依存から列名ベースへ修正
- 2.5.7/2.5.8で列ずれしたPostersデータを `upgradeV259()` で修復

## Ver.2.6.0
- ポスターが登録済みなのに一覧に出ない問題を修正
- 原因：旧東区ID `area_higashi` と現在の東区ID `fukuoka_higashi` が混在していた
- API・画面双方で旧/新活動エリアIDを同一扱い
- `upgradeV260()` でPostersの旧活動エリアIDを現在IDへ更新

## Ver.2.6.1
- ポスター登録・編集画面の上部に「活動エリア」を読み取り専用で表示
- 一般ユーザーは固定活動エリア、支部管理者等は現在選択中の活動エリアを表示
- 保存先エリアが画面内で分かるように改善

## Ver.2.6.2
- ポスター一覧の「編集」ボタンを削除し、カードクリックで編集に統一
- 地図クリック時に「訪問先として登録 / ポスターとして登録」を選択可能
- ポスター登録を選ぶと、クリック地点の住所・緯度・経度を自動入力して登録画面を開く
- 訪問先登録も従来どおり地図クリック地点の住所・位置を引き継ぐ

## Ver.2.6.3
- 自党ポスターの独立管理を廃止し、ポスタータブを削除
- 訪問先に「🍊 ポスター掲示の依頼あり」「党へ報告済み」「依頼メモ」を追加
- 他党ポスターは地図上の簡易情報として管理（党名、住所/位置、確認日、任意メモのみ）
- 地図クリック時は「訪問先として登録 / 🏳️ 他党ポスターを記録」を選択
- 自党ポスターの掲示済・貼替え・撤去などの管理は行わない

## Ver.2.6.4
- 他党ポスター記録機能を削除
- 地図の他党ポスターフィルター・アイコン・登録画面を削除
- 地図クリックは従来どおり訪問先登録へ直接進む
- 自党については訪問先の「🍊 ポスター掲示の依頼あり / 党へ報告済み / 依頼メモ」のみ残す
- 既存の Posters シートはデータ保護のため自動削除しない

## Ver.2.7.0 — UIリニューアル
- アプリ名を「アイサポ」から「あいサポ」へ変更
- オレンジの地図ピン＋人＋グリーン地図をモチーフにした新ロゴを画面へ実装
- オレンジ / グリーン / アイボリーを基調に、全画面のUIを統一
- ナビゲーションをアイコン付きの「地図 / 一覧 / 名簿 / 分析 / 管理」に刷新
- 地図画面を「地図＋フォロー・活動状況・注意」のダッシュボード構成へ変更
- 訪問先詳細を「基本情報 / フォロー / 訪問記録 / 訪問ステータス / ポスター掲示依頼 / 注意事項」のカード構成へ変更
- 党員希望 / サポーター希望 / 詳しい話を聞きたい / 対応済み / フォローメモを追加
- 一覧にフォロー対象・未対応・ポスター依頼・未報告の絞り込みを追加
- 分析にフォロー未対応・ポスター未報告を追加
- 名簿の「新規登録」を廃止。正式名簿を正として取り込み・確認に限定
- 名簿取り込みと活動エリア追加はシステム管理者のみ
- ユーザー追加画面の権限は「一般 / 支部管理者」に整理
- `upgradeV270()` を1回実行してRecordsへフォロー項目を追加

## Ver.2.7.1 — スマホ最適化
- ログイン画面をスマホ幅に最適化し、補足文「管理者が利用者と活動エリアを設定します。」を削除
- ナビゲーションの「分析」を「活動状況」に変更
- スマホではナビを5分割のアイコン付きタブに最適化
- 地図をスマホ幅いっぱいに表示し、画面高に応じて見やすいサイズへ調整
- PC右側の「フォロー / 活動状況 / 注意・未対応」カードをスマホでは地図下へ縦並び
- 訪問先詳細モーダルをスマホでは全画面表示に変更
- 保存 / キャンセルを画面下部に固定し、片手操作しやすく調整
- 一覧・活動状況・管理画面もスマホ1カラム中心に調整
- 2.7.0で `upgradeV270()` 実行済みなら追加の移行関数は不要

## Ver.2.7.2
- パスワード欄とログインボタンの間隔を20pxに調整
- 700px以下ではPCの横2カラムを強制解除し、スマホ1カラムへ変更
- 地図右側のフォロー・活動状況・注意カードをスマホでは必ず地図下へ移動
- ヘッダーをスマホではロゴ/ユーザー + 5タブの2段構成に変更
- 活動エリア、検索、フィルター、地図の横幅は画面幅100%に制限
- 横スクロールを防止

## Ver.2.7.3
- 名簿タブの「氏名・住所で検索」を削除
- 「名簿一覧を表示」ボタンと名簿カード一覧を削除
- 訪問先の検索・絞り込みは「一覧」タブへ集約
- 名簿タブは正式名簿の取り込み・登録状態に関する機能へ整理
- データ項目変更なし。upgrade関数は不要

## Ver.2.7.4 — 操作説明ツールチップ
- 地図 / 一覧 / 名簿 / 活動状況 / 管理タブに操作説明を追加
- 絞り込み / 現在地 / 更新など主要ボタンにも説明を追加
- PCはマウスホバーまたはキーボードフォーカスでツールチップ表示
- スマホは対象ボタンをタップすると画面下部に短い説明を約1.8秒表示
- データ構造変更なし。upgrade関数は不要

## Ver.2.7.5 — スマホ地図優先
- スマホの地図画面から「フォロー」「活動状況」「注意・未対応」カードを削除
- スマホでは「地図 / 訪問先を地図で確認・登録できます」の見出しを非表示
- 絞り込み / 現在地 / 更新を40pxのコンパクトアイコンボタン化
- ピン凡例と表示件数を絞り込み枠の外へ移動
- ピン凡例は「地図の見方」で開閉
- スマホヘッダーにユーザー名とログアウトを常時表示
- スマホの管理ユーザー表はカード表示へ変更し、文字つぶれを防止
- データ構造変更なし。upgrade関数不要

## Ver.2.7.6 — ログイン停止不具合修正
- 2.7.5で追加したMutationObserverによるDOM同期の無限ループを削除
- スマホ用ユーザー名はログイン完了時に1回同期
- 地図の凡例・件数はマーカー描画時に同期
- データ構造変更なし。upgrade関数不要

## Ver.2.7.7 — 地図凡例・件数の配置修正
- PCでもピン凡例と該当件数を絞り込み枠の外へ移動
- 絞り込み条件と地図の見方/件数を別ブロックに分離
- スマホは従来どおり「地図の見方」と件数を地図直前に表示
- データ構造変更なし。upgrade関数不要

## Ver.2.7.8 — 一覧から地図へ
- 地図画面の詳細フィルターを削除
- 地図は住所・名称検索 / 現在地 / 更新 / 凡例 / 件数 / 地図に整理
- 絞り込みは一覧画面に集約
- 一覧カードに「📍 地図で見る」を追加
- 「地図で見る」で地図タブへ移動し、対象地点へズームしてピン説明を表示
- 位置情報がない訪問先は「位置未取得」と表示
- データ構造変更なし。upgrade関数不要

## Ver.2.7.9 — あいサポ正式アイコン固定
- ログイン画面とアプリヘッダーのアイコンを同じデザインに統一
- オレンジの位置ピン / 向き合う2人 / 緑の地図 / 右上の小さなハート
- 「あい」はオレンジ、「サポ」は緑
- Ver.2.7.8の一覧→地図の動線もそのまま維持
- データ構造変更なし。upgrade関数不要

## Ver.2.8.0
- 支部管理者（leader）も名簿のExcel / CSV取り込みを利用可能に復旧
- 活動エリア追加は引き続きシステム管理者のみ
- あいサポの「ポ」の半濁点をオレンジのハートに変更
- ログイン画面・PC/スマホヘッダーで同じロゴ表記
- データ構造変更なし。upgrade関数不要

## Ver.2.8.1 — 地図と活動状況の役割整理
- 地図画面の人物 / 訪問状況 / フォローのチェックフィルターを完全削除
- 地図右側の「フォロー / 活動状況 / 注意・未対応」カードを完全削除
- 地図は検索 / 現在地 / 更新 / ピン凡例 / 表示件数 / 地図に集中
- フォロー / 未訪問 / 要再訪 / 訪問注意 / ポスター未報告を「活動状況」タブ上部へ移動
- 一覧画面の絞り込みと「📍 地図で見る」は維持
- データ構造変更なし。upgrade関数不要

## Ver.2.8.2
- スマホの登録ユーザー表をカード型表示へ変更し、操作ボタンの縦潰れを解消
- 活動状況を「今日やること / 訪問状況 / 対応が必要」に整理
- 今日やること：要再訪 / フォロー未対応 / ポスター依頼
- 訪問状況：未訪問 / 訪問済 / 手応え / 不在 / 要再訪 / 断られた
- 対応が必要：訪問注意 / ポスター未報告
- データ構造変更なし。upgrade関数不要

## Ver.2.8.3
- スマホの登録ユーザー「権限/エリア」を縦配置にし、活動エリア選択の横つぶれを修正
- 活動エリア追加から中心緯度・中心経度の手入力欄を削除
- 市区町村＋エリア名から中心位置を自動取得して登録
- エリア名入力欄と補足・追加ボタンの余白を調整
- スマホ地図の「現在地」「更新」を◎ / ↻が見える44pxボタンに修正
- データ構造変更なし。upgrade関数不要

## Ver.2.8.4
- スマホヘッダーにログイン中のユーザー名 / PW変更 / ログアウトを常時表示
- 一覧の詳細フィルターをスマホでは初期状態で非表示
- 「⚙ 絞り込み」を押したときだけ登録方法 / 区分 / 位置 / チェックフィルターを表示
- PCでは一覧フィルターを従来どおり常時表示
- データ構造変更なし。upgrade関数不要

## Ver.2.8.5
- スマホの訪問先詳細・地図からの新規登録モーダルを1本スクロールに修正
- 保存/キャンセル/削除エリアの不自然な重なり・二重スクロールを解消
- 詳細画面の「住所から位置取得 / 現在地から住所取得 / Googleマップで確認」ボタンを削除
- 手入力登録では、住所保存時に必要な位置情報を裏側で自動取得
- 地図から登録した位置情報はそのまま保持
- データ構造変更なし。upgrade関数不要

## Ver.2.8.6
- ① 活動エリア追加の「中心位置は自動取得します」補足を削除
- ② 訪問先詳細を「基本情報 → 訪問ステータス → 訪問記録 → フォロー」の順に変更
- ③ 一覧の📍「地図で見る」が動かなかった不具合を修正し、対象ピンへズーム
- ④ スマホでは活動エリアと重複する「全支部 / ○○区」を非表示
- ⑤ スマホの地図にも「地図 / 訪問先を地図で確認・登録できます」を表示
- ⑥ ロゴアイコンをオレンジの位置ピン＋向き合う2人＋緑の地図タイルのイメージへ変更
- データ構造変更なし。upgrade関数不要

## Ver.2.8.7
- ユーザー指定の「あいサポ」ロゴ画像をそのまま正式ロゴとして採用
- ログイン画面 / PCヘッダー / スマホヘッダーを同一ロゴ画像に統一
- SVGでの近似描画を廃止
- データ構造変更なし。upgrade関数不要

## Ver.2.8.9
- スマホの訪問日入力がカード外へはみ出す問題を修正
- 活動状況の「今日やること」を「対応が必要なもの」へ変更
- 活動状況の先頭に「支部連絡」を追加し、関係する最新5件を表示
- 支部連絡は全支部または各支部宛に件名・本文を登録可能
- 支部連絡用の BranchMessages シートを追加
- GAS更新後に upgradeV288() を1回実行してください

## Ver.2.8.10
- 地図ピンを丸みのある大きめのしずく型へ変更
- 訪問ステータスごとに白いシンプルアイコンを表示
- 訪問注意は紫の「!」補助バッジを最優先表示
- 党員は金色★、サポーターはピンク♥の小バッジ
- フォロー・ポスターの絵文字バッジを地図ピン上から外し、密集時の視認性を改善
- 地図凡例もピンと同じデザインへ変更
- データ構造変更なし。upgrade関数不要
